# Copy to .env and fill in real values locally. Never commit .env.

# packages/db
DATABASE_URL="postgresql://zuvre:zuvre@localhost:5432/zuvre_dev"

# packages/auth (Better Auth) — ADR-0005
BETTER_AUTH_SECRET="replace-with-a-real-secret"
BETTER_AUTH_URL="http://localhost:3001"

# packages/ai-gateway — ADR-0006 (add provider keys only as providers are actually wired up)
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=

# apps/web
NEXT_PUBLIC_API_URL="http://localhost:3001"
