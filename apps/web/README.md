# @zuvre/web

ZUVRE dashboard — minimal Next.js App Router shell.

## Structure
`src/app/layout.tsx`, `src/app/page.tsx` — placeholder shell only, no real dashboard UI yet.

## Ownership
Owner: `@zuvre/frontend`.

## Validation status
Authored, not executed — `next`, `react`, and their types are not installed in this build sandbox (no npm registry access). Verify with `pnpm install && pnpm --filter @zuvre/web dev` in a real environment.
