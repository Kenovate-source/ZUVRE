# @zuvre/observability

Shared structured logging conventions — the foundation observability boundary (deliberately minimal, not a full tracing/metrics platform).

## Public API
`src/logger.ts` — `Logger`, `createLogger()`, `rootLogger`.

## Ownership
Owner: `@zuvre/platform-core`.

## Validation status
Compiles cleanly with `tsc`, zero external dependencies. Both tests genuinely pass in this build.
