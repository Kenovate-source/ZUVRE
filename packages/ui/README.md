# @zuvre/ui

Shared design-system/UI components, consumed by `apps/web`.

## Public API
`src/button.tsx` — `Button` (a minimal example component proving the package boundary; not a full component library).

## Ownership
Owner: `@zuvre/frontend`.

## Validation status
Authored, not executed — `react` and `@types/react` are not installed in this build sandbox (no npm registry access). Structurally minimal so it's cheap to verify once dependencies are installed in a real environment.
